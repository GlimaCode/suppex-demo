# راه‌اندازی SUPPEX روی هاست cPanel

این راهنما فرض می‌کند هاست شما **cPanel با PHP 8.0 یا بالاتر و MySQL/MariaDB** است.
از ابتدا تا انتها حدود ۲۰ دقیقه طول می‌کشد و به هیچ ابزار خاصی نیاز ندارد.

---

## ۰. پیش‌نیازها

در cPanel این دو مورد را بررسی کنید:

| مورد | کجا | مقدار لازم |
|---|---|---|
| نسخه PHP | Select PHP Version | **8.0 یا بالاتر** |
| افزونه‌های PHP | همان صفحه | `pdo_mysql` ، `gd` ، `mbstring` ، `curl` |

افزونه `gd` برای تغییر اندازه تصاویر محصولات لازم است.
اگر فعال نبود، تیک آن را بزنید و ذخیره کنید.

---

## ۱. آپلود فایل‌ها

کل محتویات پروژه را در `public_html` قرار دهید:

```
public_html/
├── index.html
├── product.html
├── .htaccess
├── admin/
├── api/
├── assets/
├── db/
├── lib/
└── uploads/
```

**دسترسی پوشه `uploads` را روی `755` بگذارید** (در File Manager: راست‌کلیک ← Change Permissions).
تصاویر محصولات اینجا نوشته می‌شوند. `777` لازم نیست و نباید استفاده شود.

---

## ۲. ساخت دیتابیس

cPanel ← **MySQL® Databases**

1. یک دیتابیس بسازید — مثلاً `suppex`
2. یک کاربر بسازید و یک **رمز طولانی** برایش انتخاب کنید
3. **کاربر را به دیتابیس اضافه کنید و ALL PRIVILEGES بدهید**

> مرحله ۳ را جا نیندازید. ساختن دیتابیس و کاربر بدون وصل کردنشان به هم،
> شایع‌ترین دلیلی است که یک نصب تازه به دیتابیس وصل نمی‌شود.

cPanel به هر دو، نام حساب شما را به‌عنوان پیشوند اضافه می‌کند —
یعنی اسم واقعی چیزی شبیه `myaccount_suppex` می‌شود. همین نام کامل را در گام بعد بنویسید.

---

## ۳. فایل تنظیمات

فایل `config.sample.php` را باز کنید، محتوایش را کپی کنید و
فایل جدیدی به نام **`suppex-config.php`** بسازید — **یک پوشه بالاتر از `public_html`**:

```
/home/USERNAME/suppex-config.php     ← اینجا
/home/USERNAME/public_html/          ← نه اینجا
```

**چرا بیرون از public_html؟**
اگر روزی اجرای PHP خراب شود (یک `.htaccess` اشتباه، یک ارتقای ناموفق)،
هر فایل `.php` داخل `public_html` به‌صورت **متن ساده** به بازدیدکننده نمایش داده می‌شود —
از جمله رمز دیتابیس. فایلی که بیرون از پوشه وب است اصلاً آدرس اینترنتی ندارد،
پس چیزی برای نمایش دادن وجود ندارد.

سپس این چهار مقدار را پر کنید:

```php
'db' => [
  'host' => 'localhost',
  'name' => 'myaccount_suppex',   // نام کامل با پیشوند
  'user' => 'myaccount_suppex',
  'pass' => 'رمزی که ساختید',
],
```

و مسیر آپلود را با نام کاربری واقعی هاست تنظیم کنید:

```php
'uploads_dir' => '/home/USERNAME/public_html/uploads',
'uploads_url' => '/uploads',
```

---

## ۴. اجرای نصب

در مرورگر بروید به:

```
https://your-domain.ir/db/setup.php
```

فرم را پر کنید:

- **نام کاربری مدیر** — انگلیسی، بدون فاصله
- **رمز عبور** — حداقل ۱۰ کاراکتر.
  یک عبارت چندکلمه‌ای مثل `barf-mikonad-emrooz-tehran` از یک رمز کوتاهِ پیچیده امن‌تر است
  و راحت‌تر هم به یاد می‌ماند
- **محصولات نمونه** — تیک بزنید تا فروشگاه با ۱۵ محصول شروع شود.
  بعداً می‌توانید همه را از پنل ویرایش یا حذف کنید

نصب که تمام شد، پیام موفقیت را می‌بینید.

### ⚠️ حالا فایل `db/setup.php` را حذف کنید

این اسکریپت حساب مدیر می‌سازد. تا وقتی روی سرور باشد، یک صفحه ساخت حساب مدیر
روی سایت شما باز است. اسکریپت خودش جلوی اجرای دوباره را می‌گیرد،
اما حذف کردن فایل، محافظت واقعی است.

---

## ۵. اتصال فروشگاه به دیتابیس

فایل `assets/js/config.js` را باز کنید و **این یک خط** را عوض کنید:

```js
api: {
  baseUrl: 'api/index.php',      // به‌جای null
```

تا این خط عوض نشود، سایت همچنان محصولات نمونه‌ی داخل فایل را نشان می‌دهد
و **سفارش‌ها در پنل ثبت نمی‌شوند** — بدون هیچ پیام خطایی.
اگر بعد از نصب سفارشی در پنل نمی‌بینید، اول همین خط را چک کنید.

---

## ۶. تنظیمات فروشگاه

وارد `https://your-domain.ir/admin/` شوید و به **تنظیمات** بروید:

| بخش | چه چیزی |
|---|---|
| شماره کارت | ۱۶ رقم. اگر اشتباه تایپ شود، سیستم قبول نمی‌کند |
| نام صاحب حساب | دقیقاً همان‌طور که در بانک ثبت شده |
| هزینه ارسال | و سقف ارسال رایگان |
| لینک تلگرام / بله | آدرس کامل با `https://` |
| درصد سهم همکاری | روی مبلغ کالاها، نه هزینه ارسال |

شماره کارت **فقط بعد از ثبت سفارش** به مشتری نشان داده می‌شود،
نه روی صفحات عمومی سایت — تا قابل جمع‌آوری و سوءاستفاده نباشد.

---

## ۷. SSL و HTTPS

cPanel ← **SSL/TLS Status** ← گواهی رایگان AutoSSL را صادر کنید.

بعد از اینکه `https://` کار کرد، این دو کار را انجام دهید:

1. در `suppex-config.php`: `'https_only' => true`
2. در `.htaccess` بخش «Force HTTPS» را از حالت کامنت خارج کنید

> این دو را **قبل از** کار کردن گواهی انجام ندهید — سایت از دسترس خارج می‌شود
> و از پنل مدیریت بیرون می‌مانید.

---

## ۸. اطلاع‌رسانی سفارش (اختیاری)

برای اینکه لحظه ثبت سفارش خبردار شوید، در `suppex-config.php`:

```php
'telegram_bot' => [
  'enabled' => true,
  'token'   => 'توکن از @BotFather',
  'chat_id' => 'آیدی عددی چت شما',
],
```

`chat_id` را از این آدرس بگیرید (بعد از فرستادن یک پیام به ربات):
`https://api.telegram.org/bot<TOKEN>/getUpdates`

> **توکن هرگز نباید در `config.js` گذاشته شود.** آن فایل توسط هر بازدیدکننده‌ای
> دانلود می‌شود و توکن داخلش عملاً عمومی است. اینجا سمت سرور می‌ماند و به مرورگر نمی‌رسد.
> همین منطق برای کلید پنل پیامکی هم صادق است.

---

## عیب‌یابی

| نشانه | علت معمول |
|---|---|
| `SUPPEX is not configured` | فایل `suppex-config.php` ساخته نشده یا جای اشتباهی است |
| `Database unavailable` | کاربر به دیتابیس اضافه نشده، یا نام بدون پیشوند نوشته شده |
| سفارش‌ها در پنل نیست | `baseUrl` در `config.js` هنوز `null` است (گام ۵) |
| آپلود تصویر خطا می‌دهد | افزونه `gd` فعال نیست، یا دسترسی `uploads` روی 755 نیست |
| متن فارسی به‌شکل `????` | دیتابیس با `utf8mb4` ساخته نشده |
| بعد از ورود دوباره به صفحه ورود برمی‌گردد | `https_only => true` است ولی سایت روی http باز شده |
| صفحات پنل بدون ورود باز می‌شوند و بالای صفحه `Warning: headers already sent` دارند | فایل `suppex-config.php` با BOM ذخیره شده — پایین را بخوانید |

### فایل تنظیمات را حتماً «UTF-8 without BOM» ذخیره کنید

نوت‌پد ویندوز و بعضی ویرایشگرها، سه بایت نامرئی (BOM) اول فایل می‌گذارند.
PHP آن سه بایت را قبل از هر هدر برای مرورگر می‌فرستد، و نتیجه‌اش این است که
`session_start()` کار نمی‌کند و `header()` نمی‌تواند ریدایرکت کند — یعنی
**صفحات پنل مدیریت بدون ورود باز می‌شوند**.

از نسخه فعلی به بعد، `lib/bootstrap.php` این حالت را خنثی می‌کند و فقط یک
خط در لاگ خطای هاست می‌نویسد. با این حال بهتر است اصلاً اتفاق نیفتد:
در **Notepad++** یا **VS Code** فایل را با انکدینگ `UTF-8` (نه `UTF-8-BOM`) ذخیره کنید،
یا از File Manager خود cPanel استفاده کنید که BOM اضافه نمی‌کند.

---

## پشتیبان‌گیری

دو چیز باید پشتیبان گرفته شود:

1. **دیتابیس** — cPanel ← phpMyAdmin ← Export
2. **پوشه `uploads`** — تصاویر محصولات، که در دیتابیس نیستند

از cPanel ← Backup می‌توانید هر دو را با هم بگیرید.
سفارش‌ها فقط در دیتابیس هستند؛ بدون پشتیبان، قابل بازیابی نیستند.
